I have added a new secret in Replit called AI_INTEGRATIONS_GROK_API_KEY with my Grok API key from console.x.ai.
Please update the codebase in the artifacts-monorepo (pnpm workspace with Express API in artifacts/api-server and Expo mobile app) to support Grok as the primary or optional AI provider for the Master Organization Intelligence Layer and AI suggestions.
Goals:
•  Add Grok support alongside (or as replacement for) the existing OpenAI proxy (AI_INTEGRATIONS_OPENAI_*).
•  Keep a simple feature flag for easy switching.
•  Focus on structured outputs for predictable JSON responses (industry, healthcare/govcon overlays, completeness suggestions, etc.).
•  Never expose the API key client-side — all calls must go through the Express backend.
•  Enforce our product rules: AI can only suggest values (PENDING status); no silent writes to the master database. All approvals are explicitly logged.
Technical Requirements:
1.  Create / Update a helper at api-server/src/lib/grok.ts (or extend existing ai.ts if it exists):
	•  Use the official OpenAI SDK (already installed) with:
		•  baseURL: "https://api.x.ai/v1"
		•  apiKey: process.env.AI_INTEGRATIONS_GROK_API_KEY
	•  Default model: "grok-4-1-fast-reasoning" (best value for high-volume structured tasks: $0.20 input / $0.50 output per million tokens).
	•  Fallback/escalation option: "grok-4.20-reasoning" for complex healthcare/GovCon reasoning.
	•  Support structured outputs via response_format: { type: "json_object" } or JSON schema where available.
	•  Log token usage (response.usage) for cost monitoring.
2.  Update the AI Suggestions flow (/admin/ai-suggestions/generate route and related services):
	•  Add optional query param ?provider=grok (default to OpenAI for now, or make Grok default after testing).
	•  Pass relevant context: organization data (name, aliases, Google Place info, existing fields, vertical, hierarchy, scan OCR if available).
	•  Grok should return structured JSON matching our schemas for:
		•  Suggested field values (industry, account_structure_type, naicsCodes, healthcare overlay fields like facilityType/licensedBeds/traumaLevel, govcon overlay fields like uei/cageCode/naicsCodes/contractVehicles/agencyAlignment).
		•  Confidence scores per field + overall.
		•  Reasoning / explanation for admin review.
		•  Next best action and completeness improvement hints.
	•  Store suggestions as PENDING in master_org_ai_suggestions table.
3.  Add a feature flag (e.g., const AI_PROVIDER = process.env.AI_PROVIDER || 'grok'; in a config file or environment variable) so we can toggle between ‘openai’ and ‘grok’.
4.  Security & Best Practices:
	•  Read the key only from process.env.AI_INTEGRATIONS_GROK_API_KEY.
	•  Add basic error handling for rate limits (429), auth errors, etc.
	•  Keep PHI-sensitive fields out of AI calls.
	•  Log all Grok calls (model used, tokens, latency) for auditing.
5.  Mobile/Admin side:
	•  No changes needed yet (backend returns the same suggestion format).
	•  Optionally add a small badge like “Powered by Grok” in the AI suggestions screen later.
Please:
•  First show me the proposed code changes (especially the new grok.ts helper and updated suggestion route).
•  Then apply the changes.
•  Suggest a simple test endpoint or command I can run in the terminal to verify a single master-org suggestion works with Grok.
After that, we can optimize the system prompt for healthcare/GovCon accuracy, hierarchy reasoning, and our “no silent writes” rule.
Start by confirming you see the AI_INTEGRATIONS_GROK_API_KEY secret and listing the files you’ll modify.
